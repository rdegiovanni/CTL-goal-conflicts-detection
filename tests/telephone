Goals:
--G1 =
AG (calling  -> (A calling W (connected || disconnected))) ;
AG (calling  -> (AF (connected || disconnected))) ;

--G2=
AG (calling  -> (A calling W (forwarded || disconnected)));
AG (calling  -> (AF (forwarded || disconnected)))
--the desired BC
--EF (calling && EG(!disconnected))

--Domain:
--D1 = 
--AG (!connected || !forwarded) ;

--AG (!connected || !calling) ;

--AG (!forwarded || !calling) 